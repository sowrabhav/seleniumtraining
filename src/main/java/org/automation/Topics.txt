
-- *****************   Selenium WebDriver    **************
-- WebDriver - Done
-- WebElements, Locators (By.id, By.name, By.className, By.tagName, By.linkText, By.partialLinkText, By.cssSelector, By.xpath) - Done
-- WebElements (Button, CheckBox, Radio Button, Drop Down, List Box, Text Box, Text Area, Link, Image, Table, Frame, Alert, Pop Up, File Upload, File Download)
-- Actions (Mouse Hover, Drag and Drop, Double Click, Right Click, Keyboard Events)
-- Browser Commands (get, getCurrentUrl, getTitle, getPageSource, close, quit, navigate, manage, etc.)
-- Pages, IFrames, Multi tab, Popup, Alert, Window, Cookies, Screenshot, etc.
-- XPath (Absolute, Relative, Contains, Text, AND, OR, NOT, Functions, Operators, etc.) - Done
-- WebTables (Static, Dynamic, Pagination, Sorting, Filtering, etc.)
-- Synchronization (Implicit Wait, Explicit Wait, Fluent Wait, Page Load Timeout, Set Script Timeout)
-- Firebug
-- IE Developer Tools
-- Chrome Developer Tools
-- JavaScript Executor
-- AutoIT
-- Sikuli
-- Robot Class
-- Headless Browser Testing
-- Cross Browser Testing

-- All methods of webdriver, webelement, alert, actions, javascript executor, etc.
-- Selenium IDE

-- Data Driven Testing (Excel, CSV, XML, Database)

        // Types of webelements in Selenium
        // 1. Textbox - input (set the value)
        // 1a. Textarea - textarea (set the value)
        // 2. Button - button (click)
        // 3. Link - a (click)
        // 4. Image - img (click)
        // 5. Checkbox - input (click)  (select, deselect) (isSelected)
        // 6. Radio button / Radio group - input (click) (select, deselect) (isSelected)
        // 7. Dropdown - select (selectByVisibleText, selectByValue, selectByIndex)
        // 8. Listbox - select (selectByVisibleText, selectByValue, selectByIndex)
        // 9. WebTable - table (read the data)
        // 10. Frame - iframe (switchTo)
        // 11. Alert - alert (accept, dismiss, getText, sendKeys)
        // 12. File upload - input (sendKeys)
        // 13. Window - window (switchTo)



-- *****************   Frameworks    **************
-- TestNG
-- JUnit
-- Cucumber
-- POM (Page Object Model)
-- Karate (API Testing)
